<div class="friend-container">
  <div class="container mb-2">
    <div class="friend-breadcrumb">
      <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
          <li class="breadcrumb-item"><a href="/">Home</a></li>
          <li class="breadcrumb-item active" aria-current="page">フレンド一覧</li>
        </ol>
      </nav>
    </div>
  </div>
  
  <div class="friend-card">
    <div class="friend-card-header">
      <h1>フレンド一覧</h1>
    </div>
    
    <div class="friend-card-body">
      <div class="section-title">
        すべてのフレンド <span class="count-badge"><%= @friends.count %></span>
      </div>
      
      <div class="friend-list">
        <% if @friends.present? %>
          <% @friends.each do |friend| %>
            <div class="friend-list-item">
              <span class="friend-name"><%= friend.name %></span>
            </div>
          <% end %>
        <% else %>
          <div class="empty-message">
            フレンドはまだいません。「フレンド追加」から友達を追加しましょう。
          </div>
        <% end %>
      </div>
      
      <div class="section-title">保留中のリクエスト</div>
      
      <div class="friend-list">
        <% if @pending_requests.present? %>
          <% @pending_requests.each do |request| %>
            <div class="friend-list-item">
              <span class="friend-name"><%= request.requester.name %></span>
              <div class="action-buttons">
                <%= button_to '承認', accept_friendship_path(request), method: :patch, class: 'friend-btn btn-success btn-sm' %>
                <%= button_to '拒否', reject_friendship_path(request), method: :patch, class: 'friend-btn btn-danger btn-sm' %>
              </div>
            </div>
          <% end %>
        <% else %>
          <div class="empty-message">
            保留中のフレンド申請はありません
          </div>
        <% end %>
      </div>
      
      <div class="friend-actions">
        <%= link_to 'フレンド追加', friend_requests_path, class: 'friend-btn btn-primary' %>
      </div>
    </div>
  </div>
</div>
