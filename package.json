{
  "name": "travel_app",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "dependencies": {
    "@hotwired/stimulus": "^3.2.2",
    "@hotwired/turbo-rails": "^7.3.0",
    "@popperjs/core": "^2.11.8",
    "bootstrap": "^5.3.3",
    "esbuild": "0.20.0",
    "sortablejs": "^1.15.2"
  },
  "scripts": {
    "build": "esbuild app/javascript/*.* --bundle --sourcemap --outdir=app/assets/builds --loader:.js=jsx --target=es2020"
  }
}
